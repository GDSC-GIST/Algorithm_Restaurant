import sys
remain = 1000 - int(sys.stdin.readline())
n = 0  #거스름돈 개수

list1 = [500, 100, 50, 10, 5, 1]
for i in range(6):
    amount = remain//list1[i]
    n = n + amount
    remain = remain-amount*list1[i]
print(amount)
